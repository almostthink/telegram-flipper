# --- Core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
# Требуется FastAPI для приёма HAR-файла (multipart/form-data).
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0

# --- HTTP ---
httpx==0.28.1

# --- Scheduling ---
apscheduler==3.11.0

# --- Storage ---
aiosqlite==0.20.0
sqlalchemy==2.0.36

# --- Secrets (Windows DPAPI via keyring) ---
keyring==25.6.0

# --- Telegram userbot auth ---
# Входит в сборку. Ставить его отдельно бесполезно: exe несёт своё
# окружение и системный site-packages не видит, поэтому «pip install
# pyrogram» на состояние приложения никак не влияет.
#
# TgCrypto здесь намеренно нет. Это C-расширение, готовых сборок под
# свежий Python у него не выпускали, поэтому pip пытается собрать его
# сам и падает без Visual Studio Build Tools. Pyrogram работает и без
# него — только медленнее шифрует, что для одного запроса за раз
# роли не играет.
pyrogram==2.0.106

# --- Шифрование подписи Tonnel ---
# Площадка требует в теле покупки поле wtf: отметку времени, зашифрованную
# как в CryptoJS. Ставим явно, хотя Pyrogram и так тянет pyaes: полагаться
# на чужую зависимость нельзя — она может исчезнуть в любом обновлении.
pyaes==1.6.1

# --- Analytics ---
# numpy достаточно: регрессия и квантили реализованы в app/analytics.
# pandas и statsmodels добавили бы ~60 МБ к exe ради того же результата.
numpy==2.2.1
