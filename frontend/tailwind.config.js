/** @type {import('tailwindcss').Config} */

// Палитра намеренно чёрно-белая. Торговый интерфейс смотрят часами, и
// цветные акценты в нём быстро перестают что-либо значить: когда красным
// и зелёным подсвечено всё подряд, взгляд перестаёт их различать.
//
// Прибыль и убыток различаются светлотой, а не оттенком: прибыль светлее
// окружающего текста, убыток приглушён. Знак «+» или «−» стоит при числе
// всегда — смысл держится на нём, а не на цвете.
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        // Нейтральные серые без синевы: холодный оттенок к вечеру утомляет.
        ink: {
          900: '#0e0e0f', // фон страницы
          800: '#151517', // карточка
          700: '#1c1c1f', // приподнятая плашка
          600: '#26262a', // граница
          500: '#34343a', // граница поярче
        },
        // «Акцент» остался по имени, но это просто светлый серый: им
        // выделяется действие, а не привлекается внимание цветом.
        accent: '#e6e6e8',
        profit: '#f4f4f6',
        loss: '#84848c',
        warn: '#b4b4ba',
      },
      fontFamily: {
        mono: ['ui-monospace', 'SFMono-Regular', 'Menlo', 'Consolas', 'monospace'],
      },
    },
  },
  plugins: [],
}
